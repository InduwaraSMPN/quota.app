# Test Configuration

# Primary Database Configuration (Quota App) - Use the same database but with different schema initialization
spring.datasource.url=jdbc:postgresql://databasequotaapp.postgres.database.azure.com:5432/database.quota.app
spring.datasource.username=admindatabasequotaapp
spring.datasource.password=zNAj8TWuTh6:mfA
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.jdbc-url=${spring.datasource.url}

# DMT Database Configuration
spring.datasource.dmt.url=jdbc:postgresql://dmt.postgres.database.azure.com:5432/database.dmt
spring.datasource.dmt.username=admindatabasedmt
spring.datasource.dmt.password=zNAj8TWuTh6:mfA
spring.datasource.dmt.driver-class-name=org.postgresql.Driver
spring.datasource.dmt.jdbc-url=${spring.datasource.dmt.url}

# JPA Configuration for Tests
# Use validate instead of update to prevent schema changes during tests
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.physical_naming_strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.properties.hibernate.implicit_naming_strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyJpaCompliantImpl

# Server Configuration
server.port=0

# JWT Configuration
jwt.secret=TestSecretKeyForJWTThatIsLongEnoughToBeSecureInTestEnvironment
jwt.expiration=86400000

# Email Configuration - Disabled for tests
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=test@example.com
spring.mail.password=test-password
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# Session Configuration - Disabled for tests
spring.session.store-type=none
